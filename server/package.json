{
  "name": "cohort-ledger-automation-server",
  "version": "1.0.0",
  "description": "Local automation server — publishes assessments to Topin via Playwright, for Cohort Ledger",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "install-browsers": "npx playwright install chromium"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "playwright": "^1.44.0"
  }
}
